To start work on a new terra (environment type), copy the folder template first, rename it and commit that folder structure. Then start adding resources.

The "shared" folder is for stuff that works in all, or multiple, environments (e.g. small rocks).